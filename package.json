{
  "name": "richchess-serverless",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "deploy": "tgcloud push",
    "migrate": "tgcloud migrate",
    "status": "tgcloud status"
  },
  "devDependencies": {
    "@tgcloud/cli": "^0.1.2"
  }
}
